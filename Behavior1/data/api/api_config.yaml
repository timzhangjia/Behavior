# API Configuration
# Base URL and endpoint configurations

api_config:
  base_url: https://jsonplaceholder.typicode.com
  endpoints:
    posts: posts
    users: users
    comments: comments

